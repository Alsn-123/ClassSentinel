# 课堂哨兵 使用说明

常驻后台监听老师上课提问语气词（如"叫人答题""找个同学回答""谁来回答"等），
识别命中后弹悬浮窗提醒 + 震动 + 提示音，并显示老师说的那句话和之前的内容。

**识别全部在手机本地完成（sherpa-onnx 离线引擎，模型内置在 APK 里），不联网、不调用任何在线 API、不上传任何音频。**

- APK：`课堂哨兵-v1.1-debug.apk`（约 282MB，含全量中文流式识别模型 v1.1；旧的 v1.0 为小模型版本）
- 适用：Android 8.0 及以上（建议 Android 10+），arm64 真机
- 工程源码：`ClassSentinel/`（Kotlin + Compose，`./gradlew :app:assembleDebug` 构建）

## v1.1 准确率升级内容

1. **模型升级**：小模型（50MB）→ 全量双语模型 `streaming-zipformer-bilingual-zh-en-2023-02-20` int8（190MB），
   识别完整度显著提高（实测同一测试音频从碎片化误识 → 基本完整正确）。
2. **热词偏置**：你的关键词表会作为热词喂给解码器（modified_beam_search + cjkchar 建模单元，
   score=2.0），解码时向触发词倾斜——"找个同学"这类词更不容易被识别成别的词。
   热词在每次点"开始监听"时加载，改词表后重新开始监听即可生效（触发词表本身即时生效）。
3. **系统降噪**：设备支持时自动启用 NoiseSuppressor。
4. 如仍需更高准确率，可换更大模型（替换 `assets/asr-model/` 下四个文件即可）：
   `sherpa-onnx-streaming-zipformer-multi-zh-hans-2023-12-12`（中文专用，67MB）或
   `sherpa-onnx-streaming-zipformer-zh-int8-2025-06-30`（154MB，更新）。

---

## 一、安装与首次设置（重要，按顺序做）

1. **安装 APK**：把 `课堂哨兵-v1.0-debug.apk` 传到手机安装（需允许"安装未知应用"）。
2. 打开 App，按"权限检查"卡片逐项授权：
   - **麦克风**（必须）：点"去授权"允许。
   - **悬浮窗**（强烈建议）：跳转系统设置，允许"显示在其他应用上层"——
     否则锁屏/其他应用时无法弹横幅，只剩通知。
   - **通知**：允许，用于提醒兜底和常驻状态通知。
   - **电池优化白名单**：允许"忽略电池优化"，防止熄屏后监听被系统杀掉。
3. **国产 ROM 额外设置**（不做这步，锁屏几分钟后大概率被杀）：
   - 小米：设置 → 应用设置 → 应用管理 → 课堂哨兵 → 开"自启动"；省电策略 → 无限制
   - 华为/荣耀：设置 → 电池 → 启动管理 → 课堂哨兵 → 手动管理，三个开关全开
   - OPPO：设置 → 电池 → 更多设置，关闭"智能功耗控制"对本应用的限制；允许自启动
   - vivo：设置 → 电池 → 后台功耗管理，允许后台高耗电；自启动管理里允许
4. 回到 App，点 **"开始监听"**。状态栏出现常驻通知"课堂哨兵监听中"即成功，可锁屏或切换到其他应用。

## 二、日常使用

- 上课前打开 App 点"开始监听"，手机**平放桌面、尽量靠近讲台方向**（距离越近识别越准，建议 3 米内）。
- 老师说出包含关键词的话时：**悬浮横幅 + 震动 + 提示音**同时提醒，
  横幅显示老师那句话，下面一行"前文"是这句话之前的内容（老师常常先出题后点名，前文很可能就是题目）。
- 横幅 8 秒自动消失，点一下可立即关掉；同一次提醒不会在冷却时间内重复弹（默认 15 秒，可在界面调）。
- "触发历史"保留最近 50 条记录，方便回看。
- 上完课点"停止监听"。

## 三、词表与误触发调优

默认词表是 32 个短语级关键词（"找个同学""谁来回答""叫人答题"等），可在界面增删：

- **经常误提醒** → 删掉太短的词（如只留"回答"两个字的词），或加大冷却时间；
- **该提醒没提醒** → 在词表里加上老师实际说的原话关键词（越接近原话越好），
  比如你们老师习惯说"哪个同学来一下""这位同学请讲"。
- 界面上"正在听：……"会实时显示识别出的文字，方便你确认识别是否正常、该加什么词。

## 四、验证效果的两个按钮

- **测试提醒效果**：直接弹一次悬浮窗 + 震动 + 通知，用来确认提醒链路正常。
- **语音自测**（仅 debug 包）：用内置的两段音频（Windows TTS 合成的课堂场景 + 模型官方真人录音）
  完整跑一遍"模型加载 → 识别 → 关键词触发"，不需要麦克风。真人录音识别准确；TTS 合成音
  因音色机械识别偏差较大，真机用真实人声效果远好于 TTS。

## 五、常见问题

| 现象 | 处理 |
|---|---|
| 锁屏/几分钟后监听自动停 | 没做第 1 步的 ROM 自启动/电池设置；或没给电池优化白名单 |
| 只震动不弹横幅 | 悬浮窗权限没给 |
| 识别文字一直为空 | 麦克风被其他应用占用（关闭通话/录音类应用）；或手机离讲台太远 |
| 识别文字有但错字多 | 靠近讲台；在词表中加老师原话；本模型为通用模型，远场噪声大时准确率下降属正常 |
| 提醒太多 | 加大冷却时间、删短词 |

## 六、技术信息（给开发者）

- 引擎：[sherpa-onnx](https://github.com/k2-fsa/sherpa-onnx) v1.13.6（Kotlin API 源码在 `app/src/main/java/com/k2fsa/sherpa/onnx/`，native so 在 `app/src/main/jniLibs/{arm64-v8a,x86_64}`）
- 模型：`sherpa-onnx-streaming-zipformer-bilingual-zh-en-2023-02-20` int8（约 190MB，
  在 `app/src/main/assets/asr-model/`，中英双语流式，端点检测 rule1=2.4s/rule2=0.8s/rule3=20s）
- 解码：modified_beam_search（beam=4）+ 热词偏置（cjkchar 建模单元，score=2.0）；
  注意热词必须经 `createStream(hotwords)` 字符串传入，不能用 hotwordsFile（assets 模式下会 abort，见 sherpa-onnx issue #2562）
- 服务：`RecognitionService` 前台服务（`foregroundServiceType="microphone"`）+ PARTIAL_WAKE_LOCK + NoiseSuppressor，
  从主界面前台启动满足 Android 14+ 要求；START_STICKY 拉活
- 触发：`TriggerMatcher`（滚动上下文 + partial/final 双路匹配 + 冷却去重），JVM 单测 12 个全通过（`:app:testDebugUnitTest`）
- 提醒：`AlertManager`（TYPE_APPLICATION_OVERLAY 悬浮横幅 + 震动 + IMPORTANCE_HIGH 通知）
- 换更大模型：下载对应 onnx 后替换 `assets/asr-model/` 下四个文件（编码器/解码器/接合器/tokens.txt，文件名保持不变即可）
