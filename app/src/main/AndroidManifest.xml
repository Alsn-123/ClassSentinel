<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- 定向提醒（点到名字）熄屏/锁屏全屏弹出的权限；Android 14+ 默认关闭，
         首次定向提醒时系统会引导用户授权，拒绝则降级为横幅通知 -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- 仅「实验性 AI 答题」模块使用（默认关闭、需用户显式启用）；
         识别/触发/提醒核心链路永不联网 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- RomGuides 一键跳转各厂商自启动管理页所需的包可见性声明（Android 11+）。
         声明仅是"可见性"，应用不会读取这些包的任何数据。 -->
    <queries>
        <package android:name="com.miui.securitycenter" />
        <package android:name="com.huawei.systemmanager" />
        <package android:name="com.coloros.safecenter" />
        <package android:name="com.oppo.safe" />
        <package android:name="com.vivo.permissionmanager" />
        <package android:name="com.iqoo.secure" />
    </queries>

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/full_backup_content"
        android:icon="@drawable/ic_app_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".transcript.TranscriptActivity"
            android:exported="false" />

        <!-- 定向提醒全屏页：熄屏/锁屏点亮屏幕，fullScreenIntent 触发 -->
        <activity
            android:name=".alert.AlertActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:showWhenLocked="true"
            android:showOnLockScreen="true"
            android:turnScreenOn="true" />

        <service
            android:name=".service.RecognitionService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <service
            android:name=".tile.ClassSentinelTileService"
            android:exported="true"
            android:icon="@drawable/ic_app_logo"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <receiver
            android:name=".service.RestartReceiver"
            android:exported="false" />
    </application>
</manifest>
