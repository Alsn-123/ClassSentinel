# ClassSentinel 对标升级路线

> 依据：与 class-sentinel v0.3.0（全链路课堂 AI 助手）的对比分析
> 文档日期：2025-09
> 决策记录（已拍板）：① 不申请开机自启（方案 A）；② 名单匹配为 v2.0 一等公民；③ AI 答题作为可选扩展留口子，预留 API 配置（Keystore 加密）。
> 关联文档：《优化实施方案.md》为执行层方案，本文件为战略层路线，两文件同步维护。

---

## 一、定位结论

- **保持**："小而精、纯离线、一分钟上手"是护城河，不可因对标而变味。
- **借鉴**：对方在「点名质量」与「工程化」上的做法。
- **拒绝**：对方主干的联网依赖（LLM、HTTP ASR 兜底）与复杂度（多模块、提前引入 Room/WorkManager）。

---

## 二、版本路线图

| 版本 | 内容 | 对标对手 | 性质 |
|---|---|---|---|
| **v1.2** | 模型完整性校验 + 错误分类可见 + `allowBackup=false` + 日志脱敏 + CI/Lint 门禁 | 工程化 / 隐私对齐 | 快速小迭代，不动核心逻辑 |
| **v2.0** | 名单匹配（分层姓名/变体/定向检测）+ 置信度分级提醒 + 语境门控 | 点名逻辑反超 | 核心战役 |
| **v2.1+** | 本地课堂转写 + 复盘日志（此时引入 Room）+ 快捷磁贴 | 全离线转写差异化 | 按需启动 |

---

## 三、借鉴清单（做）

| 对方能力 | 我方落点 | 版本 |
|---|---|---|
| 姓名分层管理（展示名/昵称/ASR 容错变体）、定向提问检测 | 名单匹配（RosterEntry + isMe 定向提醒 + 变体进热词） | v2.0 |
| 精细 ASR 事件机制（中间结果/语句结束/错误分类） | 置信度分级提醒（利用 getResult 的 timestamps/ysProbs）+ 错误分类可见 | v2.0 / v1.2 |
| 模型安装 SHA-256 + 大小校验 | 四个模型文件完整性校验（首次校验 + 结果缓存） | v1.2 |
| CI + 单测全覆盖 + Lint 零问题 | GitHub Actions 门禁（assembleDebug + test + lint） | v1.2 |
| 隐私边界严格声明（不申请敏感权限、关闭备份、日志不记原文） | allowBackup=false、日志脱敏、"不申请权限清单"文档化、不申请开机自启 | v1.2 |
| 文档技术化（能力边界/验证状态/未完成事项） | README 增加"能力边界"章节 | v1.2 |

---

## 四、不做清单（保持定位）

| 对方功能 | 结论 | 理由 |
|---|---|---|
| LLM 答题/总结/闪卡/小测 | 不进入主线，**仅留口子**（已拍板） | 需联网 + 配置 API，破坏纯离线承诺 |
| HTTP ASR 兜底 / 断网恢复转写 | 不做 | 网络依赖，且我方无"音频导入"场景 |
| 模型在线下载 / 替换 | 不做（现有手动替换足够） | 保持 APK 开箱即用 |
| Room / WorkManager / 多模块重构 | 按需，不提前 | 单点工具的结构简单是维护优势；仅在转写/复盘日志落地时引入 Room |
| 开机自启（BOOT_COMPLETED） | **不申请**（已拍板，方案 A） | "不申请开机权限"是可写进隐私页的干净卖点，与纯离线可信任定位一致 |

---

## 五、差异化机会（对手覆盖不到的空白）

1. **全离线课堂转写**（v2.1+）：对手的转写 + AI 沉淀依赖联网 LLM，我方是唯一能打"转写也纯离线"牌的人。默认关闭、数据仅存本地。
2. **提醒体验碾压**：对手只有震动 + 通知；我方已有悬浮横幅 + 前文上下文。持续强化：名单提醒姓名高亮、置信度分级、横幅快捷操作。
3. **一分钟上手**：对手功能多必然复杂。升级原则——每加一个功能都要自问"是否破坏装完即用"；名单、校验等功能对新手必须默认可跳过。

---

## 六、AI 可选扩展的预留设计（口子）

- **配置实体与界面骨架**：`AiConfig`（baseUrl / apiKey / model / 开关），设置页预留入口（默认隐藏，标注"实验性"）。
- **密钥加密**：API Key 经 **Android Keystore（AES/GCM）** 加密后落盘，明文不出内存，对齐对手的密钥管理。
- **网络边界**：Manifest 声明 `INTERNET`（正常权限，安装无提示）；**代码路径隔离**——未启用 AI 模块时应用不发起任何网络请求，隐私页与 README 明确此承诺；识别/触发/提醒核心链路永久离线。
- **接口抽象**：`AnswerProvider` 接口 + OpenAI 兼容实现预留，便于后续接入。
- 本次仅留架构接口，不实现功能本身。

---

## 七、待办与后续

- 剩余待拍板：**门控默认值**（新装默认启用"核心词 + 语境词"门控，还是默认无语境词保持老行为？方案默认前者，附一键退回）。
- v2.1 转写是否落地、磁贴优先级，等核心功能稳定后按用户反馈决定。
