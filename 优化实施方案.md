# 课堂哨兵（ClassSentinel）优化实施方案

> 文档日期：2025-09
> 适用范围：ClassSentinel 主工程（Kotlin + Jetpack Compose，sherpa-onnx 离线语音识别）
> 方案依据：对现有代码的逐文件核对（TriggerMatcher / RecognitionService / AsrEngine / PrefsStore / MainActivity / AlertManager）
>
> 优化清单分三级：**Phase 1 高优先级**（核心体验 + 稳定性）、**Phase 2 中优先级**（工程化 + 隐私 + 体验）、**Phase 3 低优先级**（功能扩展，按需选择）。
> 实施建议：优先完成 Phase 1 三项，核心使用体验提升最明显；Phase 2 随版本迭代完善；Phase 3 等核心功能稳定后按用户反馈选择性开发。

---

## 总览：分期与文件影响

| 阶段 | 内容 | 主要改动文件 | 预计量级 |
|---|---|---|---|
| Phase 1 | 触发检测升级 / 远场识别优化 / 保活与权限引导 | 见下文 | ~7 个文件 + 单元测试 |
| Phase 2 | 工程化 / 隐私 / 体验细节 | 配置 + UI | 另立 |
| Phase 3 | 功能扩展 | 新增模块 | 按需 |

**依赖关系**：Phase 1 的 1、2、3 三块相互独立，可并行开发；其中第 1 块与现有 `TriggerMatcherTest` 耦合最紧，建议先做。

**版本映射（已拍板）**：Phase 1 + 模型校验 ≈ **v1.2 快迭代**；名单匹配 + 置信度分级 ≈ **v2.0 核心战役**；Phase 3 中的本地转写 / AI 扩展 ≈ **v2.1+ 按需**。

---

# Phase 1 详细方案

## 1. 触发检测逻辑升级（降低误触发 / 漏触发）

### 现状（问题锚点）

- `TriggerMatcher.kt`：归一化后子串匹配 + **全局冷却**（`lastTriggerAt`/`hasTriggered`）+ 同句一次（`utteranceTriggered`）+ 80 字滚动上下文 `committed`。全局冷却导致不同关键词互相压制（"谁来回答"触发后 15 秒内的"找个同学"被吞）。
- `PrefsStore.kt`：词表存为 `JSONArray<String>`，32 个默认词全平级，无类型概念。
- `RecognitionService.ACTION_APPLY_SETTINGS` 直接 `matcher = TriggerMatcher(...)` **重建实例** → 前文上下文清空、冷却清零（调整设置时可能当场重复提醒一次）。

### 方案设计

#### 1.1 数据模型：关键词分三类

```
KeywordSpec(text, type)
type ∈ { CORE 核心点名语 / CONTEXT 辅助语境 / EXCLUDE 排除词 }
```

- 持久化新 key `keyword_specs`（JSON 对象数组）；**读旧格式自动迁移**：旧 `keywords` 字符串数组整体标记为 CORE，向后兼容无损。
- 默认词表：现有 32 个全部保留为 CORE；**新增 5–7 个默认 CONTEXT**（"这道题 / 这个问题 / 接下来 / 请问 / 说说 / 讲一下 / 思考一下"，数量从严，避免门控过松）；EXCLUDE 默认空，由用户自行录入老师的口头禅。
- 事件 `TriggerEvent.keyword` 改为返回**用户原始输入词**（现有代码返回归一化词，UI 显示会失真，顺手修复）。

#### 1.2 匹配规则（核心改动）

1. **排除优先**：当前句命中任一 EXCLUDE → 本句整体屏蔽（partial 与 final 均不触发，但句子仍正常进入上下文）。
2. **语境门控**（AND 门，分级匹配）：
   - 语境词表**非空**时：核心词 + 语境词**同时命中才触发**；语境词匹配窗口 = 本句 + `committed` 前文缓冲（兼容"先出题、后点名"的跨句模式）。
   - 语境词表**为空**时：退化为纯核心词匹配（完全保留旧行为，用户可一键清空语境词回到老模式）。
3. **同句只触发一次**：保留现有 `utteranceTriggered` 机制。

#### 1.3 智能冷却（替换全局冷却）

- `lastTriggerAtByKeyword: Map<词, 时间>` **按词独立冷却**，不同关键词互不压制。
- 冷却时长按词长分级：**≥5 字 → base/2；3–4 字 → base；≤2 字 → base×2**（base = 界面滑块的 5–60s 设置值）。分级函数独立成方法，便于单测。

#### 1.4 原地更新（替换重建实例）

- `TriggerMatcher` 增加 `updateKeywordSpecs()` / `updateBaseCooldown()`，**保留** `committed`、各词冷却计时、本句状态。
- `ACTION_APPLY_SETTINGS` 改为原地更新 → 修复上下文丢失与重复提醒。

#### 1.5 UI 调整（`MainActivity`）

- 词表区改造：三档 `FilterChip`（核心/语境/排除）选择类型后添加；chips 按类型着色 + 前缀标签（如 `[排除]`），点 chip 删除。
- 门控说明文案：一行灰字解释"语境词非空时需 核心词+语境词 同时命中；清空语境词则只按核心词提醒"。
- "恢复默认词表"恢复三类默认值。

#### 1.6 热词联动（顺手收益）

- 热词 = CORE + CONTEXT（EXCLUDE 不进热词表），保持 cjkchar 纯汉字过滤不变。
- `ACTION_APPLY_SETTINGS` 同时更新热词文本，识别循环内**重建 stream**（volatile 标记）→ 解决"改词表要重启监听热词才生效"的现状限制。

### 涉及文件

`app/src/main/java/com/classguard/app/recognition/TriggerMatcher.kt`、`data/PrefsStore.kt`、`MainActivity.kt`、`service/RecognitionService.kt`、`service/AsrEngine.kt`、`app/src/test/java/com/classguard/app/TriggerMatcherTest.kt`

### 验收标准

- 单测覆盖：门控（同句/跨句）、排除屏蔽、三级冷却、原地更新、旧数据迁移、原始词展示（预计 12 → 20+ 用例）。
- 真机回归"该提醒没提醒"场景。

### 风险与缓解

门控可能漏报 → 默认语境词精简、可一键退回纯核心模式、UI 明示规则。

---

## 2. 课堂远场语音识别优化

### 现状（问题锚点）

- 降噪已具备：`RecognitionService`（L159–164）已启用系统级 `NoiseSuppressor`；但**无 AGC**，且 NS/AGC 可用性没有完整诊断与兜底。
- 端点参数（`AsrEngine.kt`）：`rule1=2.4s / rule2=0.8s+2.4s / rule3=20s`。老师讲课停顿多时 rule2 会过早断句，一句话被拆成多段，关键词可能被切到不同句导致漏触发。

### 方案设计

1. **降噪与预处理（NS + AGC，在已有降噪基础上强化）**：
   - **保留现有 NoiseSuppressor**（降噪非新增项，已生效）；
   - 补齐 `AutomaticGainControl`：`isAvailable()` 时 attach 到 `AudioRecord.audioSessionId` 并 enable，与 NS 同位置、同生命周期，`runCatching` 兜底；
   - 统一可用性诊断：logcat 打印 NS/AGC 的 `isAvailable()` 与实际创建结果，真机确认各 ROM 差异；
   - 设备差异兜底：`isAvailable()` 返回 false 时（部分国产 ROM 如此），`VOICE_RECOGNITION` 音频源通常已带 HAL 级预处理，属预期行为，不强制、不报错；
   - 备选方案（默认不做）：sherpa-onnx 自带语音降噪模型（`OfflineSpeechDenoiser`，vendored so 已含该符号），仅在实测系统 NS 无效且误识别严重时再评估——代价是额外功耗与延迟。
2. **端点检测（VAD）调优**（适配老师停顿多的特点）：
   - `rule2` 静音阈值 **0.8s → 1.2s**、最小语句长度 **2.4s → 3.0s**（减少中途停顿被误判为句末）；
   - `rule1` 2.4s → **2.2s**（略提升短句 final 的实时性）；`rule3` 保持 20s。
   - 参数集中为具名常量 + 注释写清调参依据，后续可做 A/B。
3. **禁区**：不引入能量 VAD 跳过静音——会破坏 sherpa 端点检测，明确不做。

### 涉及文件

`app/src/main/java/com/classguard/app/service/RecognitionService.kt`、`service/AsrEngine.kt`

### 验收标准

- 自测链路（`SelfTest`）跑通不退化。
- 真机 3 米实录对比：拆句数、触发召回率（记录基线）。

### 风险与缓解

阈值放宽会延迟 final 提交 → 提醒本身走 partial 实时路径不受影响；前文展示最多慢 0.4s，可接受。

---

## 3. 后台保活与权限引导强化

### 现状（问题锚点）

- 已具备：FGS(microphone) + `START_STICKY` + `PARTIAL_WAKE_LOCK` + 悬浮窗/电池白名单一键跳转。
- 问题：
  - `startAsForeground()` **无异常捕获**（Android 14+ 后台拉活会抛 `ForegroundServiceStartNotAllowedException` → 后台崩溃）；
  - 模型加载（190MB）在 `onStartCommand` 主线程执行 → 卡顿/ANR 风险；
  - 仅文字 ROM 提示，用户操作成本高、成功率低。

### 方案设计

1. **加固**：
   - `startAsForeground()` 包 `runCatching`，失败 → 置 `ServiceBus.error` + 优雅 `stopSelf()`，不再崩溃。
   - 模型加载移到 worker 线程，消除主线程卡顿；UI 增加"模型加载中"状态。
   - 新增 `monitoringEnabled` 持久化标记（PrefsStore），作为自动恢复的判定依据。
2. **自动重启恢复**：
   - `onTaskRemoved()` → `AlarmManager.setAndAllowWhileIdle` 延时 ~3s 触发 `RestartReceiver`（无需精确闹钟权限）。
   - `RestartReceiver`：标记为 true 则尝试 `startForegroundService`；**失败时（Android 14+ 后台限制）降级**为发高优先级"点按恢复监听"通知（点击回 MainActivity 由前台合法拉起）。
   - **不申请 `BOOT_COMPLETED` 开机自启**（已拍板：方案 A）。开机自启对课堂工具收益小，"不申请开机权限"是可写进隐私页的干净卖点；重启手机后由用户手动开启监听，必要时用高优先级通知提醒。
3. **一键跳转设置**：
   - 新增 `RomGuides` 工具类，按 `Build.MANUFACTURER/BRAND/DISPLAY` 识别厂商（小米 / 华为·荣耀 / OPPO·一加·realme / vivo·iQOO / 其他），映射各 ROM 自启动管理页 Intent（组件名 + `runCatching`，异常回退应用详情页 `ACTION_APPLICATION_DETAILS_SETTINGS`）。
4. **UI 与定制引导**：
   - 权限卡新增"自启动/后台管理"行（跳 `RomGuides`）。
   - "使用提示"卡按检测到的 ROM **动态渲染对应厂商的定制步骤**，替换现有固定三行文字。

### 涉及文件

`app/src/main/AndroidManifest.xml`、`service/RecognitionService.kt`、新增 `service/RestartReceiver.kt`、新增 `RomGuides.kt`、`MainActivity.kt`、`ServiceBus.kt`、`data/PrefsStore.kt`

### 验收标准

- 四个入口（自启动/电池/悬浮窗/通知）真机跳转验证。
- 杀进程、划走任务两种场景下恢复行为符合预期；重启手机后不自动恢复（明确不申请开机自启，由用户手动开启）。
- Android 14 真机全程无崩溃。

### 风险与缓解

闹钟恢复在部分 ROM 仍可能被拦 → 已设计通知降级兜底；开机自启不纳入（已拍板，方案 A）。

---

## 4. 模型完整性校验（随 v1.2 交付）

### 现状（问题锚点）

模型文件在 assets 中；损坏或缺失时 native 加载失败仅留一行 logcat，用户只看到"监听启动失败"，无法自行判断原因（对标对手的 SHA-256 + 大小校验）。

### 方案设计

- 固化 encoder / decoder / joiner / tokens 四个文件的 SHA-256 与大小常量；
- 首次启动校验，结果缓存于 prefs（不增加日常启动开销）；
- 校验失败 → `ServiceBus.error` 展示明确错误与"重新安装完整版"指引（与第 3 节错误可见性联动）；
- 校验通过后才进入模型加载流程。

### 涉及文件

新增 `service/ModelIntegrity.kt`、`service/RecognitionService.kt`、`ServiceBus.kt`

---

# v2.0 核心战役：名单匹配与置信度分级提醒（已拍板，对标升级）

> 背景：对标结论——"点名工具的本质是听到自己的名字"，短语词表覆盖不了"张三，说说你的看法"这类点名方式。名单匹配已拍板为 v2.0 一等公民。

## 1. 名单数据模型

- `RosterEntry`：`displayName`（展示名）、`aliases`（别名/昵称）、`asrVariants`（ASR 容错变体：同音字、常见误识字）、`isMe`（标记"我的名字"）。
- 存储：SharedPreferences/JSON（名单规模小，不上 Room）；支持文本批量导入（每行一条，可用"/"分隔别名）。
- 全部变体注入归一化匹配与热词表（沿用 cjkchar 纯汉字过滤）。

## 2. 匹配逻辑

- 姓名命中 + 本句/前文含疑问或指示语境 → 触发；
- `isMe` 命中 → **定向提醒**（横幅加大、姓名高亮、专属震动与文案"点到你的名字了"）；他人姓名 + 提问语境 → 普通提醒；
- 姓名触发与短语词触发共用按词独立冷却体系，姓名类默认冷却更长（减少重复轰炸）。

## 3. 置信度分级提醒

- 利用 vendored sherpa API `getResult()` 已返回的 `timestamps` / `ysProbs`（当前未利用）：取命中片段的平均置信度分档；
- 高置信 → 横幅 + 震动 + 声音全量提醒；低置信 → 仅通知轻提醒（压降识别错字导致的误触发）；
- 每次触发记录置信度与片段文本，供复盘统计各词误触发率，反哺词表调优。

## 4. UI

- 新增"名单"卡片：导入 / 增删 / 别名编辑 / "我的名字"标记。

## 5. 涉及文件

新增 `recognition/RosterMatcher.kt`、`data/PrefsStore.kt`、`MainActivity.kt`、`service/RecognitionService.kt`（置信度采集）、`service/AsrEngine.kt`（姓名热词）、对应单测。

## 6. 验收标准

- 常见姓名 + 变体 3 米实录命中率；
- 误触发对比基线（v2.0 vs v1.2 统计）；
- 置信度分档在真机上的分布合理性。

---

# Phase 2 方案（概要，实施时细化）

### 1. 工程化配置

- `.gitignore` 核对补齐（现有已覆盖 `.gradle/`、`local.properties`、`*.apk`、`.idea/` 等，检查缺漏项）。
- 单元测试扩充：`TriggerMatcher` 全路径 + 新增 `RomGuides` 意图解析纯函数测试。
- CI 门禁：GitHub Actions（assembleDebug + testDebugUnitTest + **lint，零问题才通过**），对齐对手的工程化标准。
- 本地崩溃捕获：`Thread.setDefaultUncaughtExceptionHandler` 落崩溃日志到应用私有目录（限额 + 覆盖策略），下次启动时提示并可复制。

### 2. 隐私与安全加固

- `android:allowBackup="false"`（关闭系统自动备份，避免课堂识别内容同步云端；一行改动，可随 Phase 1 一起做）。
- 日志脱敏：开发日志禁止打印完整识别文本与用户自定义词表（截断或只记长度/关键词/错误码）。
- 权限申请前说明：每次申请麦克风、悬浮窗等权限前，弹窗说明用途（首启引导页 + 各权限项说明）。
- "不申请权限清单"文档化：README + 应用内隐私页明确声明不申请存储管理、无障碍、媒体投影、开机自启（已拍板，方案 A）、通话录音等敏感权限。

### 3. 用户体验细节

- 内置新手引导：分步交互式引导（授权 → 设置 → 测试），进度存 prefs。
- 提醒方式自定义：悬浮窗/震动/提示音三开关独立（现有声音+震动，补悬浮窗开关）；震动模式 2–3 档；提示音选择；悬浮窗显示时长可调。
- 触发历史增强：搜索、单条删除、清空（现仅有全量清空）。

---

# Phase 3 清单（仅列项，按需启动）

| 项 | 要点 |
|---|---|
| 系统快捷磁贴 | `TileService` 一键开/关监听 |
| 课堂转写（可选开启） | 默认关，本地保存全文转写，支持导出复制 |
| 多场景预设 | 通识课/专业课/讲座三档灵敏度 + 词表一键切换 |
| 模型可选化 | 轻量 50MB / 全量 190MB 双安装包，或应用内下载替换 |
| AI 答题（可选扩展，已拍板：留口子） | 核心保持纯离线；用户自配 LLM 接口后才可启用；触发提问时可生成参考答案 |

### AI 可选扩展的预留设计（本次仅留架构接口，不实现功能）

- 配置实体与界面骨架：`AiConfig`（baseUrl / apiKey / model / 开关），设置页预留入口（默认隐藏，标注"实验性"）。
- 密钥加密：API Key 经 **Android Keystore（AES/GCM）** 加密后落盘，明文不出内存，对齐对手的密钥管理。
- 网络边界：Manifest 声明 `INTERNET`（正常权限，安装无提示）；**代码路径隔离**——未启用 AI 模块时应用不发起任何网络请求，隐私页与 README 明确此承诺；识别/触发/提醒核心链路永久离线。
- 接口抽象：`AnswerProvider` 接口 + OpenAI 兼容实现预留，便于后续接入。

---

# 测试与验证计划

- **单元测试**：Phase 1 第 1 块完成后重写/扩充 `TriggerMatcherTest`（预计 12 → 20+ 用例）；每次改动跑 `./gradlew :app:testDebugUnitTest`。
- **构建门禁**：每阶段末 `./gradlew :app:assembleDebug` 确认可出包。
- **真机回归清单**：
  - 默认词表漏触发回归、误触发计数；
  - 3 米实录识别准确率；
  - 锁屏 5/15/30 分钟监听存活；
  - 杀进程 / 划走任务 / 重启手机三种场景恢复验证；
  - Android 13 / 14 各一台验证。

---

# 实施顺序与门禁

| 顺序 | 交付 | 内容 | 完成门禁 |
|---|---|---|---|
| 1 | v1.2 快迭代 | Phase 1 四项（触发升级 / 远场优化 / 保活引导 / 模型校验）+ 错误可见性 + `allowBackup=false` + 日志脱敏 + CI/Lint 门禁 | 单测全绿 + assembleDebug + lint 零问题 |
| 2 | v2.0 核心战役 | 名单匹配 + 置信度分级提醒 + 语境门控收尾 | 名单/置信度单测 + 真机命中率与误触发对比基线 |
| 3 | 随版本迭代 | Phase 2 中优先级（工程化细节 / 隐私 / 体验） | 按项验收 |
| 4 | v2.1+ 按需 | 本地课堂转写 / 磁贴 / AI 可选扩展（口子） | 按用户反馈启动 |

---

# 已确认决策（2025-09 拍板）

1. **开机自启**：不纳入（方案 A）——不申请 `BOOT_COMPLETED` / `RECEIVE_BOOT_COMPLETED`，"不申请开机自启权限"写入隐私页卖点。
2. **名单匹配**：提升为 v2.0 一等公民（见"v2.0 核心战役"章节）。
3. **AI 模块**：作为可选扩展留口子，预留 API 配置（Keystore 加密），见 Phase 3 预留设计。

# 剩余待确认

1. **门控默认值**：新装用户默认启用"核心词 + 语境词"门控，还是默认无语境词（保持老行为、由用户自己加语境词）？方案默认按前者，并附带一键退回。
2. 其余事项按"实施顺序与门禁"执行。
