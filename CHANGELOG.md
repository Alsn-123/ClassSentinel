# ClassSentinel 更新日志

> 版本更新记录，按时间倒序。配套文档：《升级目录.md》（进度跟踪）、《优化实施方案.md》（执行层）。

---

## v2.4（2026-09-11）—— 转写文本清理：姓名写回真名 + 过滤英文碎片

依据真机记录（v2.3 实测）定位到两条根因。

### 根因一：内置中英双语模型会把中文音节「听成」英文单词

`tokens.txt` 实测：6257 个符号 = 5755 个中文单字 + **499 个英文 BPE 碎片**（`▁FI`、`ANCE`、`Y`、`▁ONE`…）。
远场/口音下模型会输出英文：`INANCE`(FINANCE)、`NDELIEVE`(BELIEVE)、`MILE`、`ED`、`CE`、`M`。
真机记录里的英文碎片全是噪声。

**修复**：`TextRepair.clean()` 过滤拉丁片段（中文课堂场景下几乎不可能是有效内容）。
代价：纯英文句会被清空——面向中文课堂的刻意取舍。

### 根因二：姓名纠偏只作用于触发，没写进转写

触发判定跑在流式 partial 上，而转写入库存的是更完整的 final 原文，纠正后的真名丢失。

**修复**：`RosterMatcher.repairNames()` 对 final 整句重新定位姓名并写回真名，
且不消耗冷却、不影响触发状态（`locate()` 从触发逻辑中抽出复用）。

### 覆盖范围

统一清理口径 `cleanForRecord`（姓名写回 → 卡顿折叠 → 英文碎片过滤），应用于：
实时字幕（partial）、课堂转写入库、触发历史、提醒横幅正文、语音自测输出。

### 验证

- 单元测试 94 个全绿（新增用例直接采用真机记录的乱码串），lint 零问题
- release 签名包模拟器实测：自测链路正常（`找个同学 conf=0.642`）

### 能力边界（重要）

清理让**记录可读**、让**命中姓名显示为真名**，但不会让模型「听得更准」。
`张某亦宜春一者你` 这类是声学层误识，受模型能力限制。要让某个名字更稳，
最有效的是在应用内为该名字**补充「误听变体」**（不依赖模型，见效最快）。
根本性提升需换**中文专用模型**（消除英文幻觉、中文声学更强）。

---

## v2.3（2026-09-11）—— 修复自测缺失 / 姓名识别 / 核心词不报警

### 修复的问题

**1. 正式版没有「语音自测」**

两处原因叠加：测试音频放在 `src/debug/assets`（只打进 debug 包），且 `MainActivity` 用 `BuildConfig.DEBUG` 显式屏蔽了入口。测试音频移入 `main/assets`（所有构建都包含），去掉 debug 限定。现在正式版也有自测入口。

**2. 姓名识别准确率低（阳一真 → 阳丽真）**

- **提高热词偏置**：`hotwordsScore` 2.0 → 3.5，解码器在声学相近时优先输出花名册名字。实测自测音频识别文本明显更干净，置信度 0.57 → 0.64
- **近似音匹配**：新增「声母不同、韵母相同」的宽松对齐（`易 yi` / `丽 li` 韵母同为 `i`），3 字及以上名字允许 1 处
- **收紧防误报**：姓氏读音必须完全相符；2 字名字不接受近似音；同音替换（`章三`→`张三`）任何长度仍接受
- 命中后原句错字写回真名，事件与转写显示「阳一真」

**3. 核心词（回答一下等）识别出来了却不报警**

- **语境门控改为按词判断**：4 字及以上短语（`回答一下`/`找个同学`）语义自足，不再受门控约束；仅 2~3 字短词保留门控。默认不再启用语境词
- **告警降级阈值 0.5 → 0.2**，且字面精确命中一律全量提醒；只有「猜测性命中」才按置信度降级

### 验证

- 单元测试 89 个全绿，lint 零问题
- release 签名包在模拟器实测：自测入口出现并跑通全链路；告警链路验证通过（悬浮横幅窗口 + 高优先级通知均正常）
- 同签名覆盖安装验证通过（v2.2 → v2.3 直接升级，无需卸载）

### 姓名识别的能力边界

热词偏置 + 近似音匹配能覆盖相当一部分误识，但 ASR 声学上限仍受模型与远场条件限制。某个名字若仍常被识别错，最可靠的办法是在应用内为该名字**补充「误听变体」**——这条路径不依赖模型，见效最快。

---

## v2.2（2026-09-11）—— 识别语义校验与卡顿折叠

### 修复问题
1. **单字被识别成两到三个字**（ASR 解码卡顿）
   - 现象：`动动能定定定定理`、`你来说说说说`、`请问请请这位这位同学`
   - 修复：新增 `TextRepair.collapseStutter` 折叠同字连发与相邻同片段重复
2. **音对字不对**（同音错字）
   - 现象：`张三`被识别为`章三`，按字精确匹配永远无法命中
   - 修复：读音层容错对齐（`PinyinIndex.findAlignedMatch`），同音/近音自动纠偏命中

### 新增能力
- **卡顿折叠**：连续 ≥2 个相同字符折叠为 1 个；相邻相同 2~3 字片段折叠为一份
- **叠词保护**：约 90 个常见叠词字（妈/爸/弟/妹/天/年/看/想…）两连保留，不误伤"妈妈""天天向上"
- **全链路接入**：partial 展示、final 事件、历史记录、提醒横幅、课堂转写、自测输出全部走折叠
- **名单真名写回**：名单命中后把原句错字区间整段替换为正确姓名（`张张伟`→`张伟`），转写显示真名
- **模糊惩罚置信度**：每次纠偏扣 0.1（下限 0.05）

### 已知取舍
- 强调式三连（`来来来`/`对对对`）折叠为单字
- 非叠词式动词重叠（`讨论讨论`）折叠为一份，语义轻微损失
- 白名单外的叠词名词（如人名`李丽丽`）可能被折叠，转写显示会少一个字（匹配不受影响，归一化两侧一致）

### release 签名
- 自 v2.2 起接入 release 签名（`signingConfigs.release` + 项目根 `keystore.properties`，均不入库）
- **keystore 与口令务必备份**：更换证书后已安装用户需卸载重装才能升级

---

## v2.1 —— 拼音同音匹配
- 内置 20,924 字拼音表（184KB，构建期去声调 + 声韵母混淆归一）
- 名单滑动窗口读音对齐：同音/近音错字自动命中，无需手动录错字
- 防误报：单字不参与模糊；门控/排除/按名冷却全适用；无索引退化精确匹配

---

## v2.0 —— 核心战役（点名能力）
- 名单匹配一等公民：别名 / 误听变体 / `isMe` 定向提醒
- 置信度分级提醒：高置信全量提醒，低置信降级轻提醒
- 语境门控收尾

---

## v1.2 —— 工程化 / 隐私 / 稳定性
- 关键词三级分类（CORE / CONTEXT / EXCLUDE）+ 排除优先 + 语境门控 + 按词分级冷却
- 远场识别优化：AGC + 端点检测调优
- 后台保活加固：前台服务异常兜底、模型校验、崩溃自恢复、ROM 引导
- 隐私开关：关闭备份、日志脱敏
- 工程门禁：GitHub Actions CI（assembleDebug + 单测 + lint）
