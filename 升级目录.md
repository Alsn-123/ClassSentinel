# ClassSentinel 升级目录（总索引 + 进度跟踪）

> 本文件是升级工作的总目录：一条路线看完全部升级项，并跟踪每项的进度状态。
> 配套文档：《对标升级路线.md》（战略层）、《优化实施方案.md》（执行层）。
> 状态图例：⬜ 待开工 / 🔨 进行中 / ✅ 已完成
>
> **进度总览（2026-09-11 更新）**：v1.2 ✅ · v2.0 ✅ · v2.1（拼音同音匹配）✅ ·
> v2.2（语义校验 + 卡顿折叠 + release 签名）✅ · v2.3（自测/姓名识别/漏报警修复）✅ ·
> 均已合入当前构建（单测 89 全绿 + lint 零问题 + assembleDebug/assembleRelease + 模拟器实测通过）。
> 真机项（3.3 基线对比、锁屏存活、Android 13/14 验证）待真机执行。

---

## 0. 文档导航

| 文档 | 层级 | 内容 |
|---|---|---|
| README.md | 用户层 | 项目介绍、下载安装 |
| 使用说明.md | 用户层 | 安装、权限设置与日常使用 |
| 对标升级路线.md | 战略层 | 与同类产品对比结论 + 版本路线 + 做/不做清单 |
| 优化实施方案.md | 执行层 | 各优化项的细化方案、涉及文件、验收标准 |
| **升级目录.md（本文件）** | 索引层 | 升级总目录 + 进度跟踪 |

---

## 1. 版本路线总览（更新 2026-09-11）

```
v1.2 快迭代（工程化/隐私/稳定性对齐）✅
 └─ 触发检测升级 · 远场识别优化 · 保活与权限引导 · 模型校验 · 错误可见 · 隐私开关 · CI/Lint
        ↓
v2.0 核心战役（点名能力反超）✅
 └─ 名单匹配 · 置信度分级提醒 · 语境门控收尾
        ↓
v2.1 追加（名字同音可触发）✅
 └─ 内置拼音表 · 滑动窗口读音对齐 · 同音/近音错字自动命中
        ↓
v2.2 语义校验升级（2026-09-11 交付）✅
 └─ 识别语义对齐：单字展开容错（“找个同学”→“找个一同学”）+ 读音层拦截异读
 └─ 核心词同音错字语义校验 + 模糊惩罚置信度
 └─ 本地课堂转写、快捷磁贴、AI 可选扩展、崩溃捕获
        ↓
v2.2+ 备选（未排期）
 └─ 多场景预设 · 模型可选化
```

---

## 2. v1.2 快迭代 —— 升级项目录 ✅（随 v2.2 构建交付）

### 2.1 触发检测逻辑升级 ✅
- [x] 关键词三级分类：CORE / CONTEXT / EXCLUDE（含旧数据自动迁移）
- [x] 匹配规则：排除优先 → 语境门控（可跨句 AND 门）→ 同句一次
- [x] 智能冷却：按词独立冷却，长词（≥5字）短冷、短词（≤2字）长冷
- [x] 原地更新：updateKeywordSpecs / updateBaseCooldown，保留上下文与冷却状态
- [x] 热词热更新：设置变更即重建 stream，热词即时生效
- [x] UI：三档词表 chips + 门控规则说明 + 恢复默认
- [x] 单测：门控/排除/分级冷却/原地更新/迁移/原始词展示（新增 20+ 用例）

### 2.2 课堂远场识别优化 ✅
- [x] AGC 开启（AutomaticGainControl，与 NoiseSuppressor 同会话、同生命周期）
- [x] NS/AGC 可用性诊断日志 + 设备差异兜底（不可用属预期，不报错）
- [x] 端点检测调优：rule2 1.2s / 最短语句 3.0s；rule1 2.2s（常量集中在 AsrEngine）
- [x] 自测链路回归（SelfTest 不退化，触发置信 0.57）

### 2.3 后台保活与权限引导强化 ✅
- [x] startForeground 异常加固（后台拉活失败 → 错误可见 + 优雅退出）
- [x] 模型加载移出主线程（含完整性校验）+ "模型加载中"状态
- [x] 自动重启恢复：onTaskRemoved → AlarmManager → RestartReceiver → 失败降级通知
- [x] monitoringEnabled 持久化标记
- [x] 一键跳转：RomGuides 厂商识别 + 自启动页 Intent + 异常回退应用详情页
- [x] ROM 定制引导文案动态渲染
- [x] 不申请开机自启（方案 A）

### 2.4 模型完整性校验 ✅
- [x] 四文件 SHA-256 + 大小固化（ModelIntegrity）
- [x] 首次启动校验 + 结果缓存
- [x] 失败走 ServiceBus.error 明确指引"重新安装完整版"

### 2.5 错误可见性 ✅
- [x] ServiceBus.error + modelLoading 状态流
- [x] 错误分类：模型缺失/麦克风占用/初始化失败/前台化受限
- [x] UI 错误卡展示与解决指引

### 2.6 隐私开关 ✅
- [x] android:allowBackup=false + dataExtractionRules/fullBackupContent 双关闭
- [x] 日志脱敏（只记关键词与长度，不打印识别文本/词表）
- [x] "不申请权限清单"文档化（README 能力边界 + 使用说明）

### 2.7 工程化门禁 ✅
- [x] GitHub Actions CI：assembleDebug + testDebugUnitTest + lint 零问题（.github/workflows/ci.yml）
- [x] 单测扩充（现共 64 个：触发/名单/拼音/置信度/编解码/迁移/AI/转写导出/崩溃格式化）
- [x] 本地崩溃捕获（CrashReporter，v2.2 交付：落盘 + 重启提示 + 复制清除，已真机流程验证）

---

## 3. v2.0 核心战役 —— 升级项目录 ✅

### 3.1 名单匹配（一等公民）✅
- [x] RosterEntry：displayName / aliases / asrVariants / isMe
- [x] 名单存储（SharedPreferences/JSON）+ 文本批量导入（`/` 分隔别名）
- [x] 姓名 + 语境触发；isMe → 定向提醒（红色横幅、加大字号、专属震动、专属文案）
- [x] 姓名变体注入热词表
- [x] UI：名单卡片（导入/增删/我的名字标记/删除）

### 3.2 置信度分级提醒 ✅
- [x] 采集 getResult 的 tokens/ysProbs（对数概率 → exp 概率域），命中片段分档
- [x] 高置信全量提醒；低置信降级为轻提醒
- [x] 触发记录带置信度（历史显示"置信 xx%"）

### 3.3 语境门控收尾 🔨
- [x] 门控默认值：默认启用语境门控 + 一键清空语境词退回纯核心模式（按方案默认执行）
- [ ] 真机命中率 / 误触发对比基线（**待真机执行**）

### 3.4 拼音同音匹配（v2.1 追加交付）✅
- [x] 内置 20,924 字拼音表（184KB，构建期去声调 + 声韵母混淆归一）
- [x] 名单滑动窗口读音对齐：同音/近音错字自动命中（"张三"→"章三"），无需手动录错字
- [x] 防误报：单字不参与模糊；门控/排除/按名冷却全适用；无索引退化精确匹配
- [x] 单测 12 个

---

## 4. v2.2 按需扩展 —— 升级项目录 ✅

### 4.1 本地课堂转写 ✅
- [x] 默认关闭，本地保存全文转写（引入 Room：session/segment 两表 + Repository）
- [x] 复盘页：会话列表 → 详情（触发句 ▶ 高亮）→ 复制全文 / 系统分享 / 删除 / 清空
- [x] 服务集成：final 逐句入库，触发句带关键词标记；停止时结束会话
- [x] 导出零权限（剪贴板 + ACTION_SEND，不申请存储权限）

### 4.2 系统快捷磁贴 ✅
- [x] TileService 一键开/关监听（运行中→停止；未运行→前台启动，受限降级拉起主界面自动开始）
- [x] 状态同步：服务启停 requestListeningState 刷新磁贴

### 4.3 AI 可选扩展（已拍板：仅留口子）✅
- [x] AiConfig 配置实体 + 设置页"实验功能"折叠入口（默认折叠、默认关闭）
- [x] API Key 经 Android Keystore（AES/GCM）加密存储
- [x] INTERNET 声明 + 代码路径隔离（核心链路不读取、不联网；仅"测试连接"手动触发）
- [x] AnswerProvider 接口抽象（OpenAI 兼容 HttpURLConnection 实现）+ 响应解析单测

### 4.4 识别语义校验升级（v2.2 追加交付）✅
- [x] 容错对齐匹配：名字/关键词按读音顺序对齐，允许音节间插入 1–2 个无关字（解决"单字被识别成两三个字"）
- [x] 读音层拦截：非同音字不误配；插入超预算（按名长分级）拒绝，控制误报
- [x] 语义校验下沉：预算校验内置（findAlignedMatch），名单与关键词统一走一套纠偏逻辑
- [x] 核心词也支持同音错字（"回答一下"→"回大一下"命中），替代原先仅名单可模糊匹配
- [x] 模糊惩罚置信度：同音错字/插入每处扣 0.1，阈值 0.05；低置信自动降级轻提醒（兼顾召回防误报）
- [x] 单元测试 14 个（FuzzySemanticTest）全绿

### 4.5 卡顿重复折叠（v2.2 追加交付，2026-09-11）✅
- [x] `TextRepair.collapseStutter`：同字连发连续 ≥2 个折叠为 1 个（"动动能定定定定理"→"动能定理"）
- [x] 叠词白名单：约 90 个常见叠词字（妈/爸/弟/妹/天/年/看/想…）两连保留，避免误伤正常汉语（"妈妈""天天向上"）
- [x] 相邻同片段重复折叠：2~3 字片段紧邻重复折叠为一份（"找个找个同学"→"找个同学"、"这位这位"→"这位"）
- [x] 匹配层同规则：`normalizeWithMap` 归一化时同步折叠，关键词/名单在卡顿文本下仍能命中
- [x] 全链路接入：partial 展示、final 事件、历史记录、提醒横幅、转写入库、自测输出
- [x] 名单同音纠偏写回原句：命中区间整段替换为正确姓名（"张张伟"→"张伟"），转写显示真名
- [x] 单元测试：TextRepairTest 6 个 + FuzzySemanticTest 归一化/写回用例，全量 82 个全绿
- [x] 模拟器实测回归：自测输出确认折叠生效，触发关键词正常命中

### 4.6 三项问题修复（v2.3 交付，2026-09-11）✅
- [x] **正式版自测入口**：测试音频由 `src/debug/assets` 移入 `main/assets`；`MainActivity` 去掉 `BuildConfig.DEBUG` 屏蔽（release 同样可自测）
- [x] **姓名识别提升**：`hotwordsScore` 2.0→3.5（解码器偏置，实测识别文本更干净、置信 0.57→0.64）；
      新增近似音匹配（声母不同/韵母相同，如 易 yi↔丽 li），3 字以上允许 1 处；
      姓氏必须精确命中、2 字名不接受近似音（防误报）
- [x] **核心词不漏报**：语境门控改为按词判断（≥4 字短语自足，`回答一下`/`找个同学` 不受门控）；默认不再启用语境词
- [x] **告警不再被静音**：降级阈值 0.5→0.2；字面精确命中一律全量提醒（`Confidence.shouldFullAlert` 抽为纯函数便于回归）
- [x] 单测 89 全绿（新增姓名近似音、告警决策、长核心词不漏报用例）+ lint 零问题
- [x] release 签名包模拟器实测：自测入口出现并跑通；告警链路（悬浮横幅 + 高优先级通知）验证通过

### 4.7 转写文本清理（v2.4 交付，2026-09-11）✅
真机记录（v2.3 实测）暴露的两条根因：
- [x] **双语模型英文幻觉**：`tokens.txt` 实测含 499 个英文 BPE 碎片（▁FI/ANCE/Y…），
      远场下把中文音节听成英文（INANCE←FINANCE、NDELIEVE←BELIEVE）；
      新增 `TextRepair.clean()` 只过滤**与汉字粘连**的拉丁片段与孤立单字母，
      空格分隔的正常英文词（MONDAY/IMPORTANT）保留
- [x] **姓名纠偏未进转写**：触发跑在 partial、入库用 final，纠正后的真名丢失；
      新增 `RosterMatcher.repairNames()` 对 final 重新定位并写回真名
      （`locate()` 从触发逻辑抽出复用，不消耗冷却、不影响触发状态）
- [x] 统一清理口径 `cleanForRecord`（姓名写回 → 卡顿折叠 → 英文过滤），
      覆盖实时字幕 / 转写入库 / 历史记录 / 提醒横幅 / 自测输出
- [x] 单测 94 全绿（用例直接采用真机乱码串）+ lint 零问题 + release 包模拟器自测通过
- [x] **内置姓名识别探针**：同一段含姓名语音按「无热词 / 热词 3.5 / 热词 14」跑三遍并比对，
      既能量化热词是否生效，也让用户在自己手机上可复现（素材 `assets/test/name_test.wav`）

**能力边界（实测证据）**：探针实测 `阳义真`→`阳一真`——模型**语音层已听对**
（阳义真 = yáng yì zhēn），只是把低频字「臻」写成同音字；真/珍/臻 完全同音，
纯声学模型无解，须靠读音层匹配纠回（已完成）。
热词偏置确实生效（改分数结果会变），但分数拉到 14 会让其他文本变差（`找找个同学` 重现），
故线上保持 3.5——**不要为字形精确继续加分数**。

### 4.8 备选（未排期）⬜
- [ ] 多场景预设（通识课/专业课/讲座）
- [ ] 模型可选化（轻量/全量双安装包）

---

## 5. 文件变更目录（实际交付）

### v1.2 ✅
| 类型 | 文件 |
|---|---|
| 修改 | `recognition/TriggerMatcher.kt`、`data/PrefsStore.kt`、`MainActivity.kt`、`ServiceBus.kt`、`service/RecognitionService.kt`、`service/AsrEngine.kt`、`alert/AlertManager.kt`、`AndroidManifest.xml`、`app/build.gradle.kts`、`gradle/libs.versions.toml` |
| 新增 | `recognition/KeywordSpec.kt`、`service/ModelIntegrity.kt`、`service/RestartReceiver.kt`、`RomGuides.kt` |

### v2.0 ✅
| 类型 | 文件 |
|---|---|
| 新增 | `recognition/RosterMatcher.kt`、`recognition/RosterEntry.kt`、`recognition/Confidence.kt` |
| 修改 | `data/PrefsStore.kt`、`MainActivity.kt`、`service/RecognitionService.kt`、`service/AsrEngine.kt`、`alert/AlertManager.kt` |

### v2.1（拼音同音匹配）✅
| 类型 | 文件 |
|---|---|
| 新增 | `recognition/PinyinIndex.kt`、`assets/pinyin/pinyin.txt`、`PinyinMatchTest.kt` |
| 修改 | `recognition/RosterMatcher.kt`、`service/RecognitionService.kt`、`selftest/SelfTest.kt`、`MainActivity.kt` |

### v2.2 ✅
| 类型 | 文件 |
|---|---|
| 新增 | `data/transcript/TranscriptDb.kt`（Room）、`transcript/TranscriptActivity.kt`、`tile/ClassSentinelTileService.kt`、`ai/AiConfig.kt`、`ai/AnswerProvider.kt`、`ai/KeystoreCrypto.kt`、`crash/CrashReporter.kt`、`recognition/TextRepair.kt`（卡顿折叠）、`V22FeaturesTest.kt`、`FuzzySemanticTest.kt`、`TextRepairTest.kt` |
| 修改 | `app/build.gradle.kts`（Room/KSP + release 签名配置）、`gradle/libs.versions.toml`、`.gitignore`（keystore.properties）、`AndroidManifest.xml`（INTERNET/磁贴/转写页）、`MainActivity.kt`（记录卡/AI 卡/崩溃提示/磁贴自启）、`service/RecognitionService.kt`（转写集成/磁贴刷新/折叠展示）、`selftest/SelfTest.kt`（转写入库/折叠输出）、`recognition/PinyinIndex.kt`、`recognition/TriggerMatcher.kt`（折叠归一化/拼音容错）、`recognition/RosterMatcher.kt`（区间写回真名）、`recognition/Confidence.kt`（模糊惩罚）、`recognition/TriggerEvent.kt`（fuzzyEdits）|

---

## 5.1 发布与签名（2026-09-11）

| 项 | 说明 |
|---|---|
| Release 签名 | `app/build.gradle.kts` 的 `signingConfigs.release` 从项目根 `keystore.properties` 读取凭据 |
| 密钥文件 | `classsentinel-release.jks`（自签名 RSA 2048，有效期 10000 天），**不入库**（`.gitignore` 已忽略 `*.jks` 与 `keystore.properties`） |
| 缺配置时行为 | release 回退为未签名构建，CI 与其他机器仍可 `assembleDebug`，不会因缺密钥而中断 |
| 发布产物 | GitHub Releases 同时提供 `-release.apk`（正式分发）与 `-debug.apk`（含自测音频，便于排查） |
| ⚠️ 备份 | **keystore 与口令必须妥善备份**：一旦更换证书，已安装用户需卸载重装才能升级 |

构建与校验命令：

```bash
./gradlew :app:assembleRelease          # 产出 app/build/outputs/apk/release/app-release.apk
# 校验签名（v2 方案；minSdk 26 无需 v1）
$ANDROID_HOME/build-tools/35.0.0/apksigner verify --print-certs app/build/outputs/apk/release/app-release.apk
```

---

## 6. 里程碑与门禁

| 里程碑 | 门禁 | 状态 |
|---|---|---|
| v1.2 | 单测全绿 + assembleDebug + lint 零问题 + 真机跳转/恢复验证 | 代码侧 ✅；真机项待执行 |
| v2.0 | 名单/置信度单测 + 真机命中率与误触发对比基线 | 单测 ✅；基线待真机 |
| v2.1 | 拼音匹配单测 + 模拟器回归 | ✅ |
| v2.2 | 转写/磁贴/AI/崩溃 模拟器回归 + 全量门禁 | ✅（磁贴面板实际吸附待真机验证） |
| v2.2 语义校验 + 卡顿折叠 | 单测 82 全绿 + lint 零问题 + 模拟器自测实测折叠生效 | ✅ 待真机识别率回归 |

### 真机回归清单（待执行）
- [ ] 默认词表漏触发回归、误触发计数（3.3 基线）
- [ ] 3 米实录识别准确率
- [ ] 锁屏 5/15/30 分钟监听存活
- [ ] 杀进程 / 划走任务 / 重启手机三种场景恢复验证
- [ ] Android 13 / 14 各一台验证
- [ ] 快捷磁贴添加与一键开关
